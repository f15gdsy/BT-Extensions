using UnityEngine;
using System.Collections;

namespace BT.Ex.Movement {

	public enum GeneralDirection {
		Horizontal,
		Vertical,
		Both
	}

	public enum Direction {
		Left,
		Right,
		Up,
		Down,
	}
}